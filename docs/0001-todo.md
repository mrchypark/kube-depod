좋습니다. 이제 이걸 실제로 “만드는 일감 리스트”로 쪼개보겠습니다.
아래 TODO들은 **실제 작업 순서**를 어느 정도 의식해서 정리했습니다.

---

## 0. 전체 프로젝트 준비

* [ ] Git 리포지토리 생성 (`kube-depod`)
* [ ] Rust toolchain 버전 결정 및 `rust-toolchain.toml` 작성
* [ ] 기본 Cargo 프로젝트 생성

  * [ ] `cargo init --bin kube-depod`
  * [ ] 워크스페이스 쓸지 여부 결정(추후 `engine`, `controller` 분리 가능성 고려)
* [ ] 주요 의존성 선정 및 추가

  * [ ] `kube`, `kube-derive`, `k8s-openapi`
  * [ ] `serde`, `serde_json`, `serde_yaml`
  * [ ] `tokio`
  * [ ] `thiserror` 또는 `anyhow` (에러 처리)
  * [ ] `tracing`, `tracing-subscriber` (로그)
  * [ ] `prometheus` 또는 `metrics` 관련 crates
  * [ ] `cel-rust` (표현식 엔진)
* [ ] 코드 스타일/린터 설정

  * [ ] `rustfmt`, `clippy` CI에서 돌리도록 설정

---

## 1. CRD 설계 및 구현 (DepodPolicy)

### 1.1 CRD 스키마 구체화

* [ ] `DepodPolicy` 스펙 최종 필드 정의

  * [ ] `spec.target.namespaceSelector`
  * [ ] `spec.target.podSelector`
  * [ ] `spec.trigger.annotationKey`
  * [ ] `spec.trigger.annotationValues`
  * [ ] `spec.condition.type` (`CEL` | `Builtin` 등)
  * [ ] `spec.condition.expression` (CEL 문자열)
  * [ ] `spec.action.type` (`Delete` | `Evict` | …)
  * [ ] `spec.action.gracePeriodSeconds`
  * [ ] `spec.action.dryRun`
  * [ ] `spec.limits.maxDeletesPerMinute`
  * [ ] `spec.limits.protectSystemNamespaces`
  * [ ] (선택) `status` 구조 설계 (`lastMatchTime`, `deletedPods`, `lastError` 등)

### 1.2 Rust struct 정의

* [ ] `#[derive(CustomResource)]`로 DepodPolicy 정의

  * [ ] group: `"kube-depod.io"`
  * [ ] version: `"v1alpha1"`
  * [ ] kind: `"DepodPolicy"`
* [ ] 내부 필드를 Rust 타입으로 매핑

  * [ ] Label/Namespace selector용 타입(`LabelSelector` 등) 정의
  * [ ] `ConditionType` / `ActionType` 등 enum 정의
* [ ] `DepodPolicy`에 대한 기본 validation 로직(구조 레벨) 구현

  * [ ] 빈 `annotationKey` 금지
  * [ ] `condition.type == CEL`이면 `expression` 필수 등

### 1.3 CRD 매니페스트 생성

* [ ] `kube`/`kube-derive`로부터 CRD YAML 생성 코드 작성 또는 스크립트 구성
* [ ] 생성된 CRD YAML 검토 및 수동 조정(OpenAPI schema, additionalPrinterColumns 등)
* [ ] 예시 DepodPolicy 리소스 샘플 작성 (`example-ttl-policy.yaml`)

---

## 2. kube-rs 기반 오퍼레이터 기본 골격

### 2.1 프로젝트 구조

* [ ] 디렉터리/모듈 구조 설계

  * [ ] `src/main.rs` (엔트리 포인트)
  * [ ] `src/crd.rs` (DepodPolicy 정의)
  * [ ] `src/controller.rs` (reconciler & controller)
  * [ ] `src/engine/mod.rs` (표현식/조건 엔진)
  * [ ] `src/engine/cel.rs` (cel-rust 통합)
  * [ ] `src/limits.rs` (rate limiting)
  * [ ] `src/metrics.rs`
  * [ ] `src/config.rs` (전역 설정)

### 2.2 kube-rs Controller 설정

* [ ] `Config` 로딩 (in-cluster / out-of-cluster)
* [ ] `Client` 생성
* [ ] `Api<Pod>` 및 `Api<DepodPolicy>` 준비
* [ ] `Controller::new(Pod, watcher_config)` 구성

  * [ ] `reconcile` 함수 정의
  * [ ] `error_policy` 정의
* [ ] `DepodPolicy` 변경 시 Pod 재큐를 위해 `Controller`에 `owns` or `watches` 설정 여부 검토

---

## 3. DepodPolicy 캐시 및 매칭 로직

* [ ] in-memory DepodPolicy 캐시 구조 정의

  * [ ] `Arc<RwLock<Vec<DepodPolicy>>>` 또는 전용 타입 도입
* [ ] DepodPolicy watch/reflector 구성

  * [ ] DepodPolicy 업데이트 이벤트 → 캐시에 반영
* [ ] Pod에 대해 적용 가능한 DepodPolicy 검색 함수 구현

  * [ ] `target.namespaceSelector` 필터
  * [ ] `target.podSelector`(label selector) 필터
* [ ] annotation 기반 trigger 매칭 구현

  * [ ] Pod의 `metadata.annotations`에서 `annotationKey` 확인
  * [ ] 값이 `annotationValues` 중 하나인지 확인

---

## 4. 표현식 엔진(cel-rust) 통합

### 4.1 Pod 컨텍스트 매핑

* [ ] Pod 객체를 CEL에서 사용할 수 있는 형태로 매핑

  * [ ] `serde_json::Value`로 변환
  * [ ] `object.metadata.*`, `object.status.*` 등 접근 패턴 정의
* [ ] CEL 실행 환경에 제공할 변수/함수 정의

  * [ ] `object` (Pod)
  * [ ] `now()` 또는 `now` 상수
  * [ ] `duration(...)` 함수

### 4.2 cel-rust 사용 래퍼 구현

* [ ] `compile_expression(expr: &str) -> Result<CompiledExpr, Error>` 구현
* [ ] `evaluate(compiled: &CompiledExpr, context: &PodContext) -> Result<bool, Error>` 구현
* [ ] DepodPolicy별로 표현식 캐싱 전략 설계

  * [ ] DepodPolicy 단위로 컴파일 결과 캐시
  * [ ] CRD 변경 시 캐시 무효화

### 4.3 에러 핸들링

* [ ] 표현식 파싱 실패 시 정책 비활성화/경고 로그
* [ ] 실행 중 런타임 오류 발생 시 해당 policy에 대해 삭제 수행 금지
* [ ] `status.lastError`에 에러 내용 기록

---

## 5. Reconciler 로직 구현

### 5.1 Reconcile 플로우

* [ ] Pod 이벤트 수신 시 처리 함수 구현

  * [ ] 삭제된 Pod 처리(삭제 이벤트는 무시 또는 clean-up)
* [ ] DepodPolicy 매칭

  * [ ] 캐시에서 관련 rule 목록 조회
* [ ] Trigger 확인

  * [ ] Pod annotation 확인 (`annotationKey`, `annotationValues`)
* [ ] 조건 평가

  * [ ] `condition.type == CEL`일 경우 cel-rust로 평가
  * [ ] `Builtin` 타입일 경우 별도 코드 처리
* [ ] action 실행

  * [ ] `dryRun == true`면 로그만 남기고 실제 삭제는 하지 않음
  * [ ] `action.type == Delete`일 경우 `Api<Pod>::delete` 호출
  * [ ] (후순위) `Evict` 지원 검토

### 5.2 재큐 전략

* [ ] `Action::requeue(Duration::from_secs(…))` 값 결정

  * [ ] 상태/에러에 따른 지수 백오프 고려
* [ ] transient 에러 발생 시 error_policy에서 재시도 간격 설정

---

## 6. 안전장치 (Safety Guard) 구현

### 6.1 네임스페이스 보호

* [ ] `is_system_namespace(ns: &str) -> bool` 구현

  * [ ] `kube-system`, `kube-public`, `kube-node-lease` 등
* [ ] `protectSystemNamespaces == true`인 경우 시스템 네임스페이스 Pod는 항상 skip

### 6.2 rate limiting

* [ ] 전역/DepodPolicy 단위 삭제 횟수 카운터 구조 설계

  * [ ] 간단히는 in-memory + time window
* [ ] `maxDeletesPerMinute` 초과 시 삭제 skip 및 경고 로그
* [ ] rate limit 관련 metric/로그 남기기

### 6.3 dryRun 동작

* [ ] dryRun 모드일 때:

  * [ ] 조건 평가까지는 수행
  * [ ] 실제 delete 호출은 하지 않음
  * [ ] “삭제했을 것”을 로그 및 metric으로 기록

---

## 7. Observability (Metrics, Logs)

### 7.1 Metrics

* [ ] Prometheus metric 정의

  * [ ] `kube_depod_deleted_pods_total{policy, namespace}`
  * [ ] `kube_depod_rule_matches_total{policy}`
  * [ ] `kube_depod_evaluation_errors_total{policy}`
  * [ ] `kube_depod_rate_limited_total{policy}`
* [ ] `/metrics` HTTP endpoint 구현 (예: `hyper` or `axum` + prometheus exporter)

### 7.2 Logging

* [ ] `tracing` 기반 로깅 초기화
* [ ] 주요 이벤트 로그 포인트 지정

  * [ ] rule match
  * [ ] 조건 평가 결과
  * [ ] 삭제 수행/실패
  * [ ] rate limit 발동
  * [ ] cel-rust 에러

---

## 8. 테스트 전략

### 8.1 Unit Test

* [ ] DepodPolicy 매칭 로직 테스트

  * [ ] namespaceSelector + podSelector 조합 케이스
* [ ] trigger 매칭 테스트
* [ ] CEL 표현식 평가 테스트

  * [ ] TTL, namespace 비교, phase 비교 등 대표 케이스
* [ ] rate limit 로직 테스트

### 8.2 Integration Test

* [ ] kind/minikube 환경에서 e2e 테스트 스크립트 작성

  * [ ] CRD 설치
  * [ ] kube-depod 배포
  * [ ] 테스트용 Pod/DepodPolicy 생성 → 삭제 여부 검사
* [ ] dryRun 모드 테스트
* [ ] 잘못된 표현식(파싱 에러 등) 테스트

---

## 9. 배포/운영

* [ ] Dockerfile 작성
* [ ] 컨테이너 이미지 빌드 및 레지스트리 푸시 파이프라인
* [ ] k8s Deployment/Service/Role/RoleBinding 매니페스트 작성

  * [ ] 최소 권한 원칙으로 RBAC 정의 (`get/list/watch/delete` on pods, `get/list/watch` on DepodPolicy, etc.)
* [ ] 헬름 차트 또는 Kustomize 템플릿 작성 여부 결정
* [ ] 기본 설정값(dryRun on, protectSystemNamespaces on, 낮은 rate limit)으로 초기 배포

---

## 10. 문서화

* [ ] README에 개요 및 주요 기능 설명
* [ ] `docs/` 디렉터리 생성

  * [ ] 아키텍처 설명 문서(지금 작성한 설계 문서 정리본)
  * [ ] DepodPolicy 작성 가이드
  * [ ] CEL 표현식 예제 모음
  * [ ] 운영 가이드 (dryRun → 활성화, rate limit 조정, metrics 확인 방법)

---

## 11. 단계별 실행 계획 정리 (Roadmap와 연결)

### Phase 1: 최소 동작 버전(MVP)

* [ ] Rust/kube-rs 기반 프로젝트 골격 완성
* [ ] DepodPolicy CRD 정의 및 설치
* [ ] TTL 기반 Builtin 조건 + Delete action + dryRun
* [ ] 기본 로그/간단 테스트

### Phase 2: CEL 도입

* [ ] cel-rust 통합 및 Pod context 매핑
* [ ] `condition.type = CEL` 지원
* [ ] 대표적인 CEL 예제 테스트

### Phase 3: 운영 기능

* [ ] rate limiting
* [ ] protectSystemNamespaces
* [ ] metrics 및 보다 세밀한 로그

### Phase 4: 확장 기능

* [ ] Evict action
* [ ] Policy status 필드 확장
* [ ] 복수 정책 적용, policy 우선순위 등
